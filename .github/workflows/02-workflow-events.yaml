name: 02 - Eventos
on: 
  push:
  workflow_dispatch:
  schedule:
    - cron: 00 18 * * *

jobs:
  ShowTrigger:
    runs-on: ubuntu-latest
    steps:
      - name: Event name
        run: | 
          echo "Event name: ${{ github.event_name }}"