name: 02 - Eventos
on: 
  push:
  pull_request: 
  workflow_dispatch:
  schedule:
    - cron: 00 18 * * *

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Event name
        run: | 
          echo "Event name: ${{ github.event_name }}"